# 第一队：系统研发与自动化流水线详细计划

> 适用任务：基于现有 4 个合并后的固件漏洞分析 Skill，利用 Claude Code / Hermes / DeepSeek-Harness 等智能体框架，研发一套从固件输入到漏洞候选、证据链、验证结果和分析报告的自动化流水线，并在团队过去分析过的已知漏洞固件上进行盲测能力验证。  
> 核心目标：把“几份经验型 Skill 文档”升级成“可执行、可编排、可恢复、可审计、可评价”的固件安全分析 Agent。  
> 安全边界：仅针对本地持有、明确授权的固件和隔离仿真环境；动态阶段只做非武器化、最小化验证，不生成真实攻击链、持久化、反弹 Shell 或针对公网/真实设备的利用。

---

# 一、第一队最终要交付什么

第一队最终不是交付“一个很长的 SKILL.md”，而是交付一套真正能够运行的系统。最低成果应包括：

1. 一套统一的固件安全分析 Agent 工程仓库。
2. 一个总控 Orchestrator，用于理解任务、选择 Skill、调整流程、记录决策。
3. 一组职责单一、输入输出清晰的专业 Skill。
4. 一组稳定的底层工具脚本，而不是把所有工作都交给大模型自由发挥。
5. 统一的数据 Schema，使每个 Skill 的输出可以直接成为下一阶段输入。
6. 统一日志、证据、状态恢复和失败处理机制。
7. 一套 Blind Benchmark，用团队历史漏洞固件验证 Agent 能力。
8. 自动生成的结构化分析报告。
9. 一键部署或完整部署脚本。
10. 开发文档、测试文档、用户手册、Benchmark 报告和演示材料。

比赛要求本身强调自主任务理解、多场景决策、自主执行，同时评分重点包含模块化/插件化、决策可解释性、跨工具协同和扩展性，因此第一队的研发重点必须落在“编排、决策、证据、工程化”上，而不能只把原有命令拼接成一个脚本。

---

# 二、第一队人员建议分工

假设第一队 4 人，先用 A/B/C/D 代称。

## A：队长 / 总架构 / Orchestrator 负责人

负责：

- 整体架构设计。
- 统一仓库结构。
- Skill 接口标准。
- JSON Schema。
- Orchestrator 状态机。
- Planner / Analyzer / Verifier / Reporter 的协作逻辑。
- Agent 框架接入。
- 运行状态恢复。
- 总体集成。
- Benchmark 调度。
- 最终 Demo 主流程。

A 不要求亲自实现所有模块，但必须能够看懂所有模块的输入输出，任何接口变更必须经过 A。

## B：固件解析与攻击面负责人

负责：

- 固件基线信息采集。
- 哈希、文件格式、架构、大小端检测。
- 解包和 rootfs 定位。
- 解包失败 fallback。
- Web 组件识别。
- 网络服务、启动脚本、CGI、Web 路由、UPnP/SOAP 等攻击面枚举。
- 输出 `firmware_manifest.json` 和 `attack_surface.json`。
- 相关单元测试和失败样例。

## C：二进制逆向与静态审计负责人

负责：

- ELF 自动筛选。
- Ghidra Headless 或等效反编译工作流。
- 函数、字符串、导入、调用关系提取。
- Source/Sink/Validation/Auth 特征提取。
- 调用图和数据流候选分析。
- 危险函数及厂商封装函数字典。
- 候选漏洞初筛与深度分析。
- 输出 `binary_summary.json`、`dataflow.json`、`candidate.json`。

## D：验证、Benchmark、日志与报告负责人

负责：

- QEMU/FirmAE 等隔离验证环境。
- 动态验证安全门。
- 非武器化可达性/稳定异常验证。
- Benchmark 数据集整理。
- Ground Truth 隔离。
- 自动评价脚本。
- 日志规范。
- 证据索引。
- 自动报告生成。
- 性能指标统计。
- 一键复现实验。

---

# 三、仓库必须先统一

建议仓库结构：

```text
firmware-security-agent/
├── README.md
├── AGENTS.md
├── pyproject.toml
├── requirements.txt
├── Makefile
├── .env.example
│
├── config/
│   ├── default.yaml
│   ├── safety.yaml
│   ├── models.yaml
│   └── benchmark.yaml
│
├── orchestrator/
│   ├── planner.py
│   ├── dispatcher.py
│   ├── verifier.py
│   ├── state_manager.py
│   ├── policy_engine.py
│   └── reporter.py
│
├── skills/
│   ├── 00-orchestrator/
│   ├── 01-firmware-unpack/
│   ├── 02-attack-surface/
│   ├── 03-binary-decompile/
│   ├── 04-static-analysis/
│   ├── 05-candidate-verifier/
│   ├── 06-dynamic-validation/
│   └── 07-evidence-report/
│
├── tools/
│   ├── firmware/
│   ├── filesystem/
│   ├── web/
│   ├── binary/
│   ├── emulation/
│   └── reporting/
│
├── schemas/
│   ├── firmware_manifest.schema.json
│   ├── attack_surface.schema.json
│   ├── binary_summary.schema.json
│   ├── candidate.schema.json
│   ├── evidence.schema.json
│   ├── decision.schema.json
│   └── run_state.schema.json
│
├── vendor/
│   ├── common.yaml
│   ├── tenda.yaml
│   ├── dlink.yaml
│   ├── netgear.yaml
│   └── tplink.yaml
│
├── benchmarks/
│   ├── public/
│   ├── private_ground_truth/
│   └── manifests/
│
├── evaluator/
│   ├── evaluate_run.py
│   ├── metrics.py
│   └── compare_runs.py
│
├── tests/
│   ├── unit/
│   ├── integration/
│   └── regression/
│
├── runs/
├── reports/
└── docs/
```

## 强制规则

- 原来 4 个合并 Skill 先放入 `legacy/`，只读保存。
- 后续不再直接在原 Skill 上继续堆文字。
- 所有新能力必须拆入专业 Skill 或 tools。
- Skill 描述“怎么做、何时做、失败怎么办”；脚本负责确定性执行。
- 不允许两个 Skill 同时维护同一份字段定义。
- 所有关键输出必须符合 Schema。

---

# 四、第一阶段：冻结现有 Skill 并做能力拆解

时间建议：第 1 天上午。

四个人一起完成。

## 任务 1：建立“能力矩阵”

逐份阅读现有 4 个合并 Skill，把内容拆成以下类别：

| 能力 | Skill 1 | Skill 2 | Skill 3 | Skill 4 | 最终归属 |
|---|---|---|---|---|---|
| 固件哈希 |  |  |  |  | 01 |
| 自动解包 |  |  |  |  | 01 |
| rootfs 判断 |  |  |  |  | 01 |
| Web 入口枚举 |  |  |  |  | 02 |
| Socket 服务枚举 |  |  |  |  | 02 |
| 认证边界 |  |  |  |  | 04/05 |
| 危险 Sink |  |  |  |  | 04 |
| 调用链 |  |  |  |  | 03/04 |
| 风险评分 |  |  |  |  | 05 |
| 误报排查 |  |  |  |  | 05 |
| QEMU/FirmAE |  |  |  |  | 06 |
| 报告 |  |  |  |  | 07 |
| 异常恢复 |  |  |  |  | 00 |
| 安全策略 |  |  |  |  | 00/06 |

## 任务 2：标记三类内容

每一条现有经验都标记：

- `KEEP`：直接保留。
- `MERGE`：多份 Skill 重复，需要合并。
- `REWRITE`：原方法太案例化、太硬编码，需要改成通用规则。

## 阶段验收

当天必须形成：

```text
docs/legacy_skill_capability_matrix.md
docs/skill_migration_plan.md
```

---

# 五、第二阶段：先定义统一数据接口

时间建议：第 1 天下午至第 2 天。

这是全项目最高优先级之一。

## 1. `firmware_manifest.json`

至少包含：

```json
{
  "run_id": "",
  "firmware_path": "",
  "sha256": "",
  "vendor": "",
  "model": "",
  "version": "",
  "source": "",
  "file_type": "",
  "filesystem": [],
  "rootfs_path": "",
  "architecture": "",
  "bits": 0,
  "endian": "",
  "kernel": "",
  "libc": "",
  "web_servers": [],
  "extraction_confidence": 0.0,
  "status": ""
}
```

## 2. `attack_surface.json`

每一个入口至少包含：

```json
{
  "surface_id": "",
  "category": "web|cgi|upnp|soap|socket|daemon|script",
  "protocol": "",
  "binary": "",
  "route": "",
  "handler": "",
  "input_sources": [],
  "auth_hint": "",
  "startup_evidence": [],
  "reachability_hint": "",
  "confidence": 0.0
}
```

## 3. `binary_summary.json`

至少包含：

```json
{
  "binary_id": "",
  "path": "",
  "architecture": "",
  "security_features": {},
  "imports": [],
  "strings": [],
  "functions": [],
  "network_functions": [],
  "sources": [],
  "sinks": [],
  "auth_functions": [],
  "validation_functions": []
}
```

## 4. `candidate.json`

建议核心字段：

```json
{
  "candidate_id": "",
  "surface_id": "",
  "binary_id": "",
  "entry": {},
  "source": {},
  "transform": [],
  "validation": [],
  "authorization": {},
  "sink": {},
  "call_chain": [],
  "user_control": "",
  "risk_score": 0,
  "risk_level": "",
  "evidence": [],
  "counterevidence": [],
  "confidence": "",
  "status": "new|analyzing|rejected|strong|confirmed"
}
```

## 5. `evidence.json`

每条证据统一记录：

```json
{
  "evidence_id": "",
  "stage": "",
  "type": "",
  "source_file": "",
  "command": "",
  "tool": "",
  "tool_version": "",
  "timestamp": "",
  "artifact_path": "",
  "observation": "",
  "supports": [],
  "contradicts": []
}
```

## 6. `run_state.json`

至少：

```json
{
  "run_id": "",
  "current_stage": "",
  "completed_stages": [],
  "failed_stages": [],
  "retry_count": {},
  "artifacts": {},
  "decisions": [],
  "status": ""
}
```

## 验收标准

- 每个 Schema 有示例文件。
- 每个 Schema 能通过 JSON Schema validation。
- 所有组员不得自行添加未同步字段。
- 下游模块不得直接解析上游日志文本，而应优先解析结构化 JSON。

---

# 六、第三阶段：构建 Orchestrator 总控

负责人：A。  
其他人提供各自模块状态码和异常类型。

## Orchestrator 不应只是顺序运行脚本

必须具备：

1. 任务理解。
2. 条件分支。
3. 异常处理。
4. 重试与降级。
5. 候选聚焦。
6. 动态验证门控。
7. 反证检查。
8. 结果置信度判断。
9. 中断恢复。
10. 决策日志。

## 推荐状态机

```text
INIT
 ↓
BASELINE
 ↓
UNPACK
 ├─ SUCCESS → SURFACE
 ├─ PARTIAL → FALLBACK_UNPACK
 └─ FAIL → BINARY_ONLY_MODE
 ↓
SURFACE
 ↓
BINARY_TRIAGE
 ↓
DECOMPILE
 ↓
STATIC_ANALYSIS
 ↓
RANK
 ↓
VERIFY_TOP_K
 ├─ EVIDENCE_SUFFICIENT → REPORT
 ├─ EVIDENCE_INSUFFICIENT → LOCAL_VALIDATION
 └─ FALSE_POSITIVE → NEXT_CANDIDATE
 ↓
REPORT
 ↓
DONE
```

## 每次决策必须记录

```json
{
  "decision_id": "",
  "stage": "",
  "inputs": [],
  "observation": "",
  "options": [],
  "selected": "",
  "reason": "",
  "confidence": 0.0,
  "next_stage": ""
}
```

注意：报告中展示的是“可审计的决策摘要”，不是模型隐式思维过程。

---

# 七、第四阶段：实现固件解包 Skill

负责人：B。

## 输入

```text
firmware_path
output_dir
vendor(optional)
model(optional)
```

## 自动流程

### Step 1：基线记录

- 文件名。
- 文件大小。
- SHA256。
- `file` 类型。
- 前若干字节特征。
- binwalk 扫描结果。
- 执行时间。
- 工具版本。

### Step 2：解包

按检测结果选择：

- binwalk。
- unsquashfs。
- sasquatch。
- dd 切片。
- 其他必要的只读本地解析方式。

### Step 3：rootfs 评分

检测：

- `/bin`
- `/sbin`
- `/etc`
- `/lib`
- `/usr`
- `/www`
- `/htdocs`
- `/etc/init.d`

多个候选时不能简单选第一个，应输出候选列表及评分。

### Step 4：架构检测

从多个 ELF 交叉确认：

- MIPS / ARM / x86 / PPC。
- 32/64 位。
- 大小端。
- libc。
- kernel 线索。

### Step 5：异常降级

必须支持：

```text
binwalk 无结果
→ 检查魔数
→ 定位文件系统偏移
→ 手工切片模块
→ 再尝试文件系统工具
```

仍失败则输出：

```text
status = partial/failed
```

并允许总控进入 `BINARY_ONLY_MODE`，不能整个系统直接退出。

## 输出

- `firmware_manifest.json`
- `unpack.log`
- rootfs
- `unpack_evidence.json`

## 验收

至少找 3 种不同封装固件测试：

- 正常 SquashFS。
- 有厂商头部。
- 自动解包失败但 fallback 成功。

---

# 八、第五阶段：攻击面自动枚举 Skill

负责人：B。

## 必须覆盖两类入口

### A. Web/CGI/UPnP/SOAP

检查：

- Web 根目录。
- CGI。
- Lua/PHP/Python/Shell 脚本。
- httpd/goahead/uhttpd/boa/lighttpd/nginx 等组件。
- 路由表。
- handler 注册。
- UPnP/SOAP/SSDP/HNAP。
- 环境变量和 HTTP 参数。
- 启动脚本中的 Web 服务。

### B. 非 Web 网络服务

检查：

- socket。
- bind。
- listen。
- accept。
- recv。
- recvfrom。
- UDP/TCP daemon。
- 启动脚本。
- 固定端口字符串。
- IPC/本地服务与外部网络服务区分。

## 关键设计

不能只扫描 webroot。

应同时：

```text
文件系统入口枚举
+
二进制注册函数/字符串反推
+
启动脚本
+
监听函数线索
```

## 输出

`attack_surface.json`

## 验收

抽取历史固件进行人工对照，目标不是“条目越多越好”，而是：

- 真正网络可达入口尽量不漏。
- 明显内部工具不要全部当外部攻击面。
- 每个入口有来源证据。

---

# 九、第六阶段：二进制反编译 Skill

负责人：C。

这是当前已有 Skill 相对薄弱、但老师新任务明确要求补齐的部分。

## 第一层：二进制筛选

不要反编译 rootfs 全部 ELF。

先对所有 ELF 打分：

- 是否被启动脚本调用。
- 是否网络相关。
- 是否 Web handler。
- 是否导入危险函数。
- 是否包含 CGI/route/HTTP/UPnP 字符串。
- 是否有解析用户输入函数。

只对 Top-N 进入深度反编译。

## 第二层：Headless 反编译

优先考虑：

```text
Ghidra Headless
```

批量导出：

- 函数列表。
- 函数地址。
- 调用关系。
- 反编译文本。
- 字符串引用。
- imports。
- exports。
- xrefs。

## 第三层：结构化摘要

不要把几万行反编译代码直接塞给 Agent。

先转成：

```json
{
  "function": "",
  "callers": [],
  "callees": [],
  "strings": [],
  "sources": [],
  "sinks": [],
  "auth_checks": [],
  "validation_checks": []
}
```

## 第四层：调用图

构建：

```text
entry → helper1 → helper2 → sink
```

允许 BFS/DFS 搜索多层间接调用。

## 验收

对历史已知漏洞二进制：

- 能找到目标函数或相关处理链。
- 即使符号被 strip，也能通过字符串/xref/调用关系保留候选。
- 不能因为没有函数名就整个分析失败。

---

# 十、第七阶段：静态漏洞审计 Skill

负责人：C。

## 分析模型统一采用

```text
Entry
→ Source
→ Transform
→ Validation
→ Authorization
→ Sink
→ Reachability
→ Counterevidence
→ Conclusion
```

## Source

重点看：

- HTTP 参数。
- Header。
- Cookie。
- Query。
- POST body。
- SOAP 参数。
- socket buffer。
- 配置导入。
- 文件上传。
- 环境变量。

## Sink

按类别维护：

### 命令执行类

只做静态定位和安全验证，不生成武器化利用链。

### 内存安全类

关注：

- 不安全字符串复制。
- 格式化。
- 长度与边界。
- 用户长度如何传递。

### 文件系统类

关注：

- 可控路径。
- 文件写入。
- 配置覆盖。
- 脚本生成。

### 格式串/解析类

关注：

- 可控格式。
- parser 边界处理。
- 长度字段。
- 类型转换。

## Validation

查：

- 长度检查。
- 白名单。
- 黑名单。
- 编码。
- 参数类型限制。
- 路径规范化。
- 字符过滤。
- 上游过滤。

## Authorization

查：

- session。
- login state。
- auth helper。
- route 级认证。
- handler 内认证。
- 初始化/健康检查例外。

---

# 十一、第八阶段：候选风险评分

负责人：C，A 审核。

不要把“看到 system/strcpy”直接等同于漏洞。

建议统一评分考虑：

- P：预认证可达性。
- I：输入来源。
- U：用户可控程度。
- D：危险 Sink 可达性。
- C：拼接/边界危险程度。
- S：Shell 或危险解释上下文。
- W：文件写入。
- K：配置持久化相关性。
- V：输入校验强弱。
- T：可测试性。

## 输出

```text
candidate_ranking.json
```

## Agent 策略

```text
候选 ≤ 5
→ 全部进入 Verifier

候选 6~20
→ Top-5

候选 > 20
→ Top-3 + 类别多样性保留
```

防止排名前几名全是同一类重复告警。

---

# 十二、第九阶段：Verifier 反证审查

负责人：A + C。

这一模块必须独立于初始 Analyzer。

对每个高风险候选逐条问：

1. Source 真的是外部输入吗？
2. 输入确实用户可控吗？
3. 变量是否真的到达 Sink？
4. 中间有没有编码或白名单？
5. 调用链是否真实可达？
6. handler 是否启动？
7. 是否需要认证？
8. 是否仅调试功能？
9. 是否存在构建条件或平台条件？
10. 有没有证据与结论矛盾？

## Verifier 输出

```text
ACCEPT
DOWNGRADE
REJECT
NEED_DYNAMIC
```

并记录：

- 原评分。
- 修正评分。
- 原因。
- 支持证据。
- 反证。

---

# 十三、第十阶段：动态验证模块

负责人：D。

## 原则

只用于：

- 团队过去已知漏洞固件。
- 本地授权测试固件。
- 隔离 QEMU/FirmAE/离线 harness。

不连接真实生产设备或公网。

## 建议分层

### L0：不启动目标

仅静态证据。

### L1：二进制可加载

确认：

- 架构。
- loader。
- 动态库。
- 基本启动。

### L2：服务可达

确认：

- 服务启动。
- 私有 IP。
- 端口。
- 基线请求。
- 正常响应。

### L3：非武器化验证

只确认：

- 某路径是否被执行。
- 某参数是否到达代码点。
- 是否稳定产生异常。
- 是否出现可重复 crash/日志/marker。

## 强制安全门

进入动态阶段前检查：

```text
AUTHORIZED == true
AND
LOCAL_LAB == true
AND
PRIVATE_NETWORK == true
AND
BASELINE_READY == true
```

不满足则：

```text
ABORT_DYNAMIC_VALIDATION
```

---

# 十四、第十一阶段：日志和证据系统

负责人：D。

所有模块都必须遵循：

```text
动作 → 证据 → 判断
```

## 每一步记录

- 时间。
- run_id。
- 工作目录。
- 工具。
- 工具版本。
- 命令或操作。
- 输入文件 SHA256。
- 退出码。
- stdout/stderr。
- 原始结果路径。
- 结构化摘要。
- Agent 判断。
- 置信度。

## Run 目录

```text
runs/<run_id>/
├── input/
├── logs/
├── artifacts/
├── state/
├── evidence/
├── decisions/
├── candidates/
└── report/
```

---

# 十五、第十二阶段：Benchmark 体系

负责人：D，A 协助。

老师要求在大家过去挖出过漏洞的固件上做能力验证，必须做成真正的盲测，而不是把 CVE 信息提前喂给 Agent。

## 1. Benchmark 数据区

```text
benchmarks/public/case01/
├── firmware.bin
└── manifest.yaml
```

Agent 能看。

## 2. Ground Truth

```text
benchmarks/private_ground_truth/case01.yaml
```

Agent 运行时禁止读取。

Ground Truth 包含：

- 真正相关二进制。
- 真正入口。
- 真正关键函数/处理链。
- 漏洞类型。
- 认证状态。
- 已知根因。
- 可接受的匹配规则。

## 3. Blind Run

```text
固件
→ Agent
→ 生成 Top-K
→ 冻结输出
→ Evaluator 读取 Ground Truth
→ 自动评分
```

---

# 十六、Benchmark 指标

至少统计：

## 1. Unpack Success Rate

成功获得可信 rootfs 的比例。

## 2. Attack Surface Recall

Ground Truth 对应入口是否出现在自动攻击面中。

## 3. Vulnerable Binary Rank

真正相关二进制排第几。

## 4. Candidate Top-K Hit

真正根因是否进入：

- Top-1
- Top-3
- Top-5
- Top-10

## 5. Root Cause Match

是否找对：

```text
Source → Sink
```

或等价关键根因。

## 6. Authentication Judgment Accuracy

preauth/auth 判断是否正确。

## 7. False Positive Count

最终高风险候选中误报数量。

## 8. Time to First Useful Finding

从输入固件到第一个有效候选的时间。

## 9. Human Intervention Count

人工介入次数。

## 10. Reproducibility

同固件重复运行两次：

- Top-K 是否大致稳定。
- 核心证据是否一致。

---

# 十七、第十三阶段：Agent 框架接入

负责人：A。

第一阶段只选一个主要研发框架，不要同时维护三套。

建议开发时先完成：

```text
Framework Adapter
```

接口例如：

```python
class AgentRuntime:
    def run_skill(self, skill_name, context):
        ...
    def call_tool(self, tool_name, args):
        ...
    def save_state(self, state):
        ...
```

然后：

```text
adapters/
├── claude_code/
├── hermes/
└── deepseek_harness/
```

系统业务逻辑与具体大模型解耦。

## 为什么必须解耦

最终比赛环境可能限制模型 API，因此：

- Skill 不能只依赖某个模型独有能力。
- Tool 接口必须标准。
- Prompt 和模型配置独立。
- Runtime 可替换。
- 核心状态和证据必须落盘。

---

# 十八、第十四阶段：自动报告

负责人：D。

最终报告自动生成以下部分：

1. 任务范围与授权边界。
2. 固件信息。
3. 文件哈希。
4. 解包结果。
5. 架构/系统组件。
6. 攻击面。
7. 二进制分析。
8. 候选排行。
9. 主候选数据流。
10. 认证边界。
11. 输入校验。
12. 支持证据。
13. 反证。
14. 本地验证结果。
15. 结论置信度。
16. 限制。
17. 修复建议。
18. 运行指标。
19. 人工介入点。
20. 完整证据索引。

---

# 十九、失败恢复机制必须实现

至少覆盖：

| 异常 | 自动策略 |
|---|---|
| binwalk 失败 | 魔数/偏移 fallback |
| rootfs 不明确 | 候选 rootfs 评分 |
| 无 Web | 转 socket/daemon |
| 无符号 | strings/xref/调用图 |
| Ghidra 导入失败 | 基础 ELF 模式继续 |
| QEMU 不支持 | 静态结论 + 标记动态未完成 |
| Web 不通 | 停止触发，排查启动/网络 |
| 候选过多 | 风险评分 Top-K |
| 候选全被否决 | 输出“未发现强证据”，不能强行报漏洞 |
| Agent 中断 | 从 run_state 恢复 |

---

# 二十、测试体系

## 单元测试

分别测试：

- 哈希。
- 文件格式识别。
- rootfs 评分。
- ELF 架构。
- 危险函数分类。
- Schema validation。
- 私有 IP 判断。
- 风险评分。

## 集成测试

至少：

```text
firmware.bin
→ unpack
→ surface
→ binary
→ rank
```

## 回归测试

每修改 Skill 后，自动重跑 Benchmark 子集，防止“修一个品牌，另一个品牌坏了”。

---

# 二十一、每天的工作节奏

## 上午

- 10 分钟同步。
- 昨天完成什么。
- 今天要交付什么。
- 当前阻塞。
- 接口是否变化。

## 下午

每个人必须至少提交：

```text
代码/Skill/测试/文档
```

中的可验证产物。

## 晚上

队长跑：

```text
integration smoke test
```

并更新：

```text
docs/progress.md
```

---

# 二十二、从 8 月 17 日开始的建议排期

## 8 月 17 日

- 建仓库。
- 冻结 legacy Skill。
- 能力矩阵。
- 确定 8 个新 Skill。
- 确定 Schema。
- Benchmark 历史固件清单。

**当天验收：架构文档 + 仓库骨架 + Schema v0.1。**

## 8 月 18 日

- 完成 `firmware_manifest`。
- 完成自动解包 MVP。
- 完成 run state。
- 完成日志基本框架。

**验收：一个固件从输入跑到 rootfs + JSON。**

## 8 月 19 日

- 攻击面枚举。
- Web + socket 双路径。
- 自动生成 `attack_surface.json`。

**验收：至少一个固件成功输出结构化攻击面。**

## 8 月 20 日

- ELF 初筛。
- Headless decompile MVP。
- 函数/字符串/调用关系结构化。

**验收：关键 ELF 自动生成 `binary_summary.json`。**

## 8 月 21 日

- Source/Sink。
- 数据流。
- 风险评分。
- Top-K。

**验收：固件自动生成 Top-3 候选。**

## 8 月 22 日

- Verifier。
- 认证检查。
- 误报修正。
- 反证记录。

**验收：Top-3 每个候选都有支持证据和反证检查。**

## 8 月 23 日

- Benchmark Runner。
- Ground Truth 隔离。
- 第一轮 Blind Run。

**验收：至少跑完 2 个历史漏洞案例。**

## 8 月 24 日

- 修历史 Blind Run 暴露的问题。
- 增加异常恢复。
- 增加 vendor adapter。

## 8 月 25 日

- 扩大 Benchmark。
- 统计 Top-1/3/5。
- 统计误报。
- 统计人工介入。

## 8 月 26 日

- 动态验证框架。
- 安全门。
- 基线连通性。
- 日志。

## 8 月 27 日

- 第二队真实使用反馈接入。
- 修新固件中暴露的兼容问题。

## 8 月 28–29 日

- 集成优化。
- Agent 编排优化。
- Tool/Skill 边界整理。
- 减少硬编码。

## 8 月 30 日

- Agent v0.9。
- 全量历史 Benchmark。

## 8 月 31 日

- 冻结大架构。
- 后续只允许修 bug 和小范围规则。

## 9 月 1–2 日

- Agent v1.0。
- 重新跑全部 Benchmark。
- 生成最终性能表。
- 保存完整演示 run。

## 9 月 3–4 日

- 一键部署。
- README。
- 用户手册。
- 开发文档。
- 测试报告。
- 技术报告相关部分。
- 演示视频素材。

---

# 二十三、第一队每个人最终交付清单

## A

- [ ] 系统架构文档
- [ ] Orchestrator
- [ ] 状态机
- [ ] policy engine
- [ ] runtime adapter
- [ ] 决策日志
- [ ] 总体集成测试

## B

- [ ] Firmware Unpack Skill
- [ ] Attack Surface Skill
- [ ] rootfs fallback
- [ ] Web/socket 双路径
- [ ] 两类 JSON 输出
- [ ] 相关测试

## C

- [ ] Binary Decompile Skill
- [ ] Static Analysis Skill
- [ ] 调用图
- [ ] Source/Sink
- [ ] Auth/Validation 分析
- [ ] Risk Ranking
- [ ] Verifier 辅助规则

## D

- [ ] Local Validation Skill
- [ ] Benchmark Runner
- [ ] Evaluator
- [ ] Evidence System
- [ ] Report Generator
- [ ] Regression Test
- [ ] 性能表

---

# 二十四、P0 / P1 / P2 优先级

## P0：必须完成

- [ ] 固件自动解包。
- [ ] 攻击面枚举。
- [ ] 关键 ELF 筛选。
- [ ] 静态候选分析。
- [ ] Top-K。
- [ ] 结构化证据。
- [ ] Orchestrator。
- [ ] Benchmark。
- [ ] 报告。
- [ ] 中断恢复。

## P1：强烈建议

- [ ] Headless Decompiler 深度集成。
- [ ] Verifier。
- [ ] Vendor Adapter。
- [ ] 安全动态验证。
- [ ] 自动回归测试。
- [ ] Agent Runtime 抽象。

## P2：时间允许再做

- [ ] 多 Agent 并行。
- [ ] Web UI。
- [ ] 图形化调用图。
- [ ] 长期 Skill 自进化数据库。
- [ ] 多模型自动路由。

---

# 二十五、第一队成功标准

第一队不能以“代码写完”作为成功。

真正成功必须满足：

```text
任意一份受支持的 Linux 路由器固件
↓
自动建立基线
↓
自动解包
↓
自动攻击面枚举
↓
自动筛选关键二进制
↓
自动反编译/结构化
↓
自动形成候选
↓
自动 Top-K
↓
自动反证检查
↓
必要时进入安全本地验证
↓
生成证据报告
```

并且：

- 发生中间错误能够降级或恢复。
- 每个结论都有证据。
- 不发现漏洞时允许输出“未发现强证据”。
- 已知漏洞 Blind Benchmark 中，真正根因能够较稳定进入 Top-K。
- 第二队能够直接使用，而不需要第一队成员全程陪跑。

---

# 二十六、禁止事项

- 不要继续把所有经验堆进一个超级 Skill。
- 不要让每个模块自己定义输入输出。
- 不要把模型生成的自然语言直接当下游唯一输入。
- 不要为了 Benchmark 分数把 CVE 名、PoC 或 Ground Truth 暗示给 Agent。
- 不要看到危险函数就直接判漏洞。
- 不要为了“动态验证成功”扩大为攻击链。
- 不要对真实设备、公网或未授权目标进行主动测试。
- 不要没有日志就认定运行成功。
- 不要临近提交再统一文档和部署。

---

# 二十七、第一队明天立刻执行的 Checklist

- [ ] 4 人角色正式确定。
- [ ] 创建 Git 仓库。
- [ ] legacy 4 Skill 只读归档。
- [ ] 建 `skills/ tools/ schemas/ orchestrator/ benchmarks/ evaluator/ tests/`。
- [ ] 完成能力矩阵。
- [ ] 完成 5~7 个核心 JSON Schema。
- [ ] 选一个历史固件作为集成测试样本。
- [ ] 跑通 `固件 → rootfs → firmware_manifest.json`。
- [ ] 定义 Orchestrator 状态机。
- [ ] 定义每个阶段成功/失败状态码。
- [ ] 建 `run_state.json`。
- [ ] 建 `docs/progress.md`。
- [ ] 第二天开始前确保所有人能 clone、安装依赖并运行 smoke test。

---

# 二十八、最后一句话

第一队接下来所有工作的核心评价标准只有一个：

> **能不能把“人过去怎么挖洞的经验”转化成一套 Agent 可调用、工具可执行、过程可追踪、失败可恢复、结果可评价的自动化能力。**

只要这一点做成，第一队就完成了老师这次任务中最核心的“系统研发”部分。
