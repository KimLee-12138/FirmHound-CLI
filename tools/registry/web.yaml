tools:
  - name: tools.web.build_attack_surface
    module_path: tools.web.pipeline
    function_name: execute_surface
    description: Build attack surface from rootfs inventory and webroot.
    args_schema:
      type: object
      properties:
        run_dir:
          type: string
      required: [run_dir]
